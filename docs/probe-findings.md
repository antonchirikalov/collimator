# Результаты пробника (этап 0)

Заполняется после `/probe probe-runs/first`. Доказательства и откаты по каждому пункту —
в `docs/handoff.md`. Пока не заполнено, этап 1 не начинается: генератор проектируется по
фактам, а не по предположениям.

Прогон: `probe-runs/…` · дата: · итог: `{ … }` (что вернул скрипт)

| № | Неизвестное | Итог | Что видно как доказательство | Что это меняет |
|---|---|---|---|---|
| 1 | Наш рукописный скрипт запускается как `/probe` | | | |
| 2 | `agentType` подхватывает `.claude/agents/probe-researcher.md` | | | |
| 3 | Агент воркфлоу дотягивается до Tavily MCP | | | |
| 4 | Артефакты пишутся в каталог прогона | | | |
| 5 | Гейт работает отдельной стадией | | | |
| 6 | Петля гейт-ретрая доводит артефакт | | | |
| 7 | Каталог прогона приходит через `args` | | | |

## Наблюдения, которых не ждали

<!-- всё, что удивило: поведение схемы, формулировки ошибок, стоимость, время стадий -->

## Решение по этапу 1

<!-- какие места генератора меняются против docs/plan.md; если пункт 2 не подтвердился —
     промпты вставляются в вызовы целиком, и это переписывает emit_agents.py -->
